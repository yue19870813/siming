# 司命桌面应用

正式桌面应用骨架，使用 React 19、TypeScript、Vite、Tailwind CSS 4 和 Tauri 2。

```bash
npm install
npm run dev
npm run tauri -- dev
```

质量检查：

```bash
npm run format:check
npm run lint
npm run typecheck
npm test
npm run build
```

`prototype/` 是产品交互稿与在线预览，不参与正式桌面应用构建。
