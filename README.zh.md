README.zh.md

TODO：
1. 需要增加一个项目设置和用户设置，设置内容包括：项目文件保存位置、导出文件位置（支持配置相对路径，方便多人协作）等；
2. 导出按钮旁边加一个小的下拉菜单以支持导出其他格式，比如XML、二进制等；
3. 画布要支持鼠标滚轮或者触摸板的放大缩小，以及画布的拖动；
4. 每个node节点都支持配置一个自定义消息（Event，与业务层面的事件做区分），用于广播给上层宿主（Unity、UE等）做业务逻辑；
5. 除了“画布”和“数据”模式外，增加一个“表格”模式，方便按顺序阅读对话内容（需要设计一下表格如何显示分支）；
6. 侧边属性菜单，在不选中对话的节点时，默认显示这个对话文件的熟悉设置，比如（描述、标签等等）
